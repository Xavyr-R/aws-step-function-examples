{
  "Comment": "Success state machine, showing how to use the success type. Note that by default if a workflow reaches the end state it will be marked as successful, this is just to make it explicit",
  "StartAt": "Success",
  "States": {
    "Success": {
      "Type": "Succeed",
      "Comment": "This indicates that the step function has executed successfully. Note that this is only to make it explicit as the step function counts as successful if all steps are executed without failure anyway"
    }
  }
}